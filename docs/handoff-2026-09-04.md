# Handoff: 2026-09-04 session

This records the original handoff, the follow-on implementation, and the work that remains.
It is authoritative for the next session. Read the whole file before touching code:
the **Decisions** section holds calls only the operator can make, the **Decided** section holds
calls already made so they are not reopened, and each item under **Gaps** and **Opportunities**
names the files, the acceptance criterion and how to verify it.

Working agreement for the follow-on work is the global `AGENTS.md`: TaskSpindle is the route for
another model's voice, opposite-provider review before accepting, one short imperative commit
sentence, no push, PR, or deployment without explicit approval.

## State

### Latest follow-up: Grok continuation acceptance complete

The follow-up branch `grok-session-continuation` starts at `58360fe`. Correlation of
the two failed TaskSpindle continuations (`ts_fb1a272a216e`, `ts_1febadddabde`) and
the independent direct ACP failure established the same sequence in all three:
Grok loaded the session, accepted the next prompt, started inference, and retried
its Responses transport request until the caller deadline cancelled the turn.
The native logs recorded seven, eight and six retries respectively. This isolates
the recorded stalls to native Grok response transport, without establishing why
that transport failed. It does not support a TaskSpindle continuation fix.

A fresh direct control with Grok CLI 1.0.13 completed an initial turn and two
fresh-process `session/load` plus prompt continuations using the production argv
and allowlisted environment. Both focused TaskSpindle resume tests passed.
`Settings fetch failed` appears in successful runs as well; it is not a sufficient
cause. No retry, fallback, timeout or provider-selection behavior was changed.

The isolated 0.2.0 FastMCP check passed using real user systemd workers and existing
Grok OAuth: initial consult plus two `continue_task` turns all reached COMPLETED,
retained the synthetic token and reused session `01a06e7c-d80d-7212-bbbf-8bce126172d0`.
Each turn reported one model call and `acp_turn_completed` usage:

| Turn | Input tokens | Output tokens |
| --- | --- | --- |
| Initial | 14521 | 47 |
| First continuation | 14595 | 50 |
| Second continuation | 14673 | 50 |

`usage_report` counted three turns, 43789 input tokens and 147 output tokens.
Cleanup reached COMPLETE with nothing retained and no validation worker left.
The check used isolated configuration, state and a scratch repository; the active
0.1.0 MCP registration was not switched. This closes the recorded continuation
acceptance gap; it does not promise that provider transport cannot fail again.

Sanitized investigation, the reusable driver and results are retained privately
under `~/.local/state/codex-feature-adoption/grok-investigation/`. Publication,
private merges, activation and installed Rutherford removal still require
explicit operator approval.

### Complete 0.2.0 plan: implemented locally, release held

Worktree `~/personal_projects/taskspindle-release-readiness`, branch `release-readiness`,
started from the merged/pushed `c4f6c30` baseline: Ruff clean, **410 passed, 1 skipped**.
Final behavior checks: Ruff clean, **418 passed, 1 skipped**. JavaScript syntax and targeted
web/report Pyright pass (zero errors). Real stdio smoke lists **17 tools**. The built 0.2.0
wheel was installed separately and served HTML, JavaScript and CSS with HTTP 200 and content.
A temporary XDG installation of adapter 0.70.0 passed all **13 non-live doctor checks**.
The pinned 0.1.0 runtime, active Codex configuration and installed Rutherford remain untouched.

| Finished work | Commit |
| --- | --- |
| Repository path labels with stable-ID fallback and No repository bucket | `8faca84` |
| Observe adapter-denied delegation and deduplicate by tool-call ID | `932cb13` |
| Record successful Grok resumed-turn usage measurements | `42956bf` |
| Search tasks and compare candidate diffs with matching reviews | `db2af2c` |
| Opt-in discovery initialize probes | `b820380` |
| Prepare unpublished 0.2.0 metadata and documentation | `02a3539` |

- **Grok accounting measurement completed:** TaskSpindle task `ts_6dbb39e6326f` completed an
  initial turn and two continuations in session `01a06e37-6fd6-7851-8330-34d2c5026b61`.
  Input/output counts were 14500/34, 14561/32 and 14620/27, each with modelCalls=1 and source
  `acp_turn_completed`. These observations support turn-local accounting; no subtraction added.
  Direct ACP probes also completed same-process second turns. One fresh-process load replayed
  ten updates and then timed out at 90 seconds; a separate fresh-process retry succeeded.
  The latest follow-up above isolates these recorded stalls to native response transport.
  **Publication and activation still require the operator's approval.**
- **Opportunity 7 completed:** the real Claude adapter denied a Task call before the permission
  callback. The initial canary exposed missing telemetry; the fake-agent reproduction failed
  twice before the observer fix. The repeated real canary recorded `DELEGATION_ATTEMPT` and
  created no subagent. Exact canonical initial tool titles avoid unrelated Task-prefixed commands.
- **Opportunity 4 completed:** literal, case-insensitive Unicode search covers task ID and
  prompt, combines with existing filters before limiting, and preserves typing focus. Candidate
  SHA/revision guards prevent mixing stale diff and review content. Finding links target diff
  lines; desktop columns stack on narrow screens. Playwright checked search, finding navigation,
  responsive layout and scroll/focus retention across polling, including a 500-line diff.
- **Opportunity 6 completed:** default discovery still launches no processes. `--probe` performs
  initialize only, with bounded timeout and per-agent errors, without signing in, prompting or
  changing profiles. Live initialization passed for Gemini CLI 0.58.0, Grok and OpenCode 1.18.23.
  This is not second-class profile or consult verification.
- **Opportunity 2 implemented, activation pending:** private workspace-config branch
  `taskspindle-release` contains `15fbb87` (observed acceptance audit) and `f254aa4` (activation
  preparation and Rutherford registration retirement). The opt-in JSON seed preserves existing
  runtime hooks; `--check-registration` detects missing/divergent registration. The asynchronous
  observer distinguishes requested from confirmed acceptance, reads the store without writing it,
  records integrated SHA and actual commit subject when available, and deduplicates observations.
  It is an observed audit: clients must poll task_status through ACCEPTED. It is not a background
  completion service. Relevant private tests: **49 passed**. Native hook activation and its fresh
  Codex acceptance cycle remain part of the explicitly gated deployment instructions in private
  `docs/taskspindle-activation.md`.
- Private codex-runtime branch `taskspindle-release` removes managed Rutherford sources in
  `bde36c9` and prepares the immutable final public SHA with runtime version 0.2.0. Four runtime
  tests pass. Installation, private merges and active registration changes remain gated. The
  primary codex-runtime checkout retains its unrelated Rutherford edit. Final inspection found
  workspace-config clean with sonnet configured; its historical Claude edit is absent. Rutherford has
  a private 0600 backup at `~/.local/state/taskspindle-release-readiness/` before any removal.

#### Live cycle and review dispositions

The branch executable and explicit TASKSPINDLE_CONFIG drove a throwaway repository with
`__pycache__/` ignored. Claude consult `ts_c63a25971635`, superseded canary `ts_9f63c324ae5e`,
Grok consult `ts_6dbb39e6326f`, fixed canary `ts_4f33bb2e6685` and Grok candidate review
`ts_5463b78ed93d` constitute **five** tasks. Grok passed the candidate without findings.
Receipt 12 covered the full 256-byte diff, read before acceptance. Acceptance landed
`29cdaed0f5edd0282302f77ecf2d477a9f96ca93`; HEAD and function behavior were confirmed.
The superseded candidate was rejected, all five tasks were force-cleaned with nothing retained,
and all six fixture grants were revoked.

All 19 tasks from this run (five live-cycle tasks and fourteen review tasks) are terminal with
cleanup COMPLETE. Review permissions on the public and two private repositories are revoked.
The named browser session and web server stopped; throwaway repositories and preview
installations were removed. Review results and cleanup evidence are retained privately in
`~/.local/state/taskspindle-release-readiness/evidence/`.

Every substantive change received a TaskSpindle opposite-provider review:

| Review | Disposition |
| --- | --- |
| Claude `ts_f48ee0a32c01`, labels | Fixed stale CLI expectation and header; retained ID fallback because common_dir cannot reliably identify a worktree path. |
| Claude `ts_fd3f6b4a3f29`, denial telemetry | Fixed title-prefix false positives, deduplicated only matching tool IDs, retained independent violations, and corrected annotations. |
| Grok `ts_83346a851c6d`, audit | Replaced whole-file hook ownership with opt-in JSON seed; retained accepted SHA if Git subject lookup fails. |
| Claude `ts_79ab5481060a`, dashboard | Reproduced scroll reset twice and fixed it; removed stale branch, limited columns to real diffs, and fixed Unicode case matching. |
| Claude `ts_02d81fce1729`, discovery | Added explicit timeout duration, uniform result fields and cleanup-failure classification. |
| Claude `ts_42f410c7f8d3`, runtime retirement | Fixed README introduction, documented approved installed-path cleanup, and asserted source-directory removal. |
| Claude `ts_cfd026f5cd56`, registration | Documented install-before-switch prerequisite; absent 0.2.0 live paths remain an activation gate. Fixed stale wording, punctuation, unused imports and duplicate assertion. |
| Claude `ts_bd6410e21d91`, audit confirmation | Added registration check and ownership/scope docs; fixed multi-block response parsing and test placement. Declined background completion polling because the agreed feature audits observed results and requires task_status confirmation. |
| Grok `ts_560201705e05`, JSON seed | Added the missing JSON legacy-merge rejection test. |
| Grok `ts_a463a758daae`, release metadata | Marked 0.2.0 explicitly prepared and unpublished while retaining the release-workflow heading; corrected usage documentation and the capabilities version example. |
| Grok `ts_24dc1c0fbffc`, runtime pin (BLOCK) | Retained unreachable-upstream-SHA and unpublished-version findings as deployment blockers. The approved scope prepares a local 0.2.0 pin; publication must precede private merge/sync. Do not substitute old main or activate this pin early. |

Initial Claude audit `ts_0fe287b90840` and Grok audit `ts_1bc4b778a20b` reached TURN_TIMEOUT
without verdicts; completed reviews above replace them. Premature runtime-pin review `ts_97dc6025ab32` was cancelled because its snapshot preceded the successful public metadata commit.
Root-mutation warnings on snapshot reviews correspond to the owner's concurrent working-tree
edits; they are not evidence of reviewer writes. No implementation candidate from these source
reviews was accepted or integrated.

### Earlier follow-on execution (historical)

Final branch checks: Ruff clean; **410 passed, 1 skipped**. Targeted dashboard/report Pyright:
zero errors. Real stdio smoke: 17 tools. The installed-wheel static-asset check passed; JavaScript
syntax passed after repository grouping. The baseline was 388 passed, 1 skipped, as required.
All 20 tasks started during the follow-on run are terminal with cleanup COMPLETE; the three
throwaway repositories and this checkout have no active grants from this run. Throwaway
repositories and temporary drivers/installations were removed. All substantive changes received
TaskSpindle review; every finding is addressed or explicitly dispositioned below.
That execution did not publish or activate. Subsequently the operator approved fast-forward
merge, push and cleanup: main reached `c4f6c30` and the merged feature branch was removed.
The later release-readiness work below supersedes its pending-decision status.

| Item | Commit |
| --- | --- |
| Gap 1: diff paging measurement | `e069d0a` |
| Gap 2: resumed-usage blocker | `1dc78b6` |
| Gap 3: Claude session metadata | `0e12857` |
| Gap 4: next-pin prerequisite | `276a24b` |
| Gap 6: dashboard typing | `b7e9ed0` |
| Gap 7: Starlette watch | `b687d53` |
| Gap 8: retain revoke interface | `dc61cd3` |
| Gap 9: clean verification docs | `19f46c4` |
| Gap 10: context parity check | `9e20a19` |
| Opportunity 1: recovery elicitation | `d9c21f5` |
| Opportunity 3: repository grouping | `6a7be7f` |
| Opportunity 5: Grok attribution | `2fd0bc5` |

Each item has its own short, no-body commit, followed by green Ruff and full pytest checks.
The handoff update is a separate final commit.

- Baseline reconfirmed: Ruff clean; 388 passed, 1 skipped.
- **Gap 1 completed:** the native Codex `task_diff(length=16384)` response arrived intact;
  decoding gave exactly 16384 bytes from a 36200-byte diff. Default remains 16384. Receipts
  7, 8 and 9 covered lengths 16384, 16384 and 3432. This proves this fixture, not a universal
  token bound; the tool documentation now states that limitation.
  Branch-driven cycle: Claude consult `ts_072fb7efd9b5`, Grok consult `ts_90a78b356f05`, Grok
  candidate `ts_3601a00769a1`, Claude review `ts_f29e0175301c` (PASS, no findings).
  Acceptance landed `56f6745e459b84772510d8de9aecc775aa36aec8` in the throwaway repository.
  All four tasks report cleanup COMPLETE with nothing retained; all six repository grants were
  revoked and the throwaway repository was removed. Only the explicitly approved native diff
  call used the registered server; the pinned runtime and Codex configuration were untouched.
- **Gap 3 completed:** canonical Claude model IDs from ACP session configuration now precede
  the session-file fallback on new and loaded sessions. Six changed-behavior cases cover the
  parser, fallback and persisted attribution. Adapter 0.70.0 populates `config_options`, but this
  seat returned `default` with description `Opus (1M context)` and alias choices, even when
  `claude-opus-5` was requested. Those names cannot establish a canonical ID; the file fallback
  remains necessary. No model is guessed from a display name.
  Branch-driven cycle: Claude consult `ts_5af79f79c6d6`, Grok consult `ts_a062f64dcdbd`, Claude
  candidate `ts_a3065058122c`, diagnostic consult `ts_821a994bb016`, Grok review
  `ts_7dac479d7fc2` (PASS, no findings). Receipt 11 covered the entire 257-byte diff; acceptance
  landed `ce13c41c5491c0bbba073ffa047aa6ad100cf8da`. All five tasks were force-cleaned,
  all six grants revoked, and the throwaway removed.
  Source review `ts_09fc07ca7798` (Grok) stalled and was cancelled; explicit Claude review
  `ts_10d5d818d8d2` passed. Its three low findings are dispositioned: grouped/option-level
  descriptions are deferred because the observed adapter uses flat choices and noncanonical
  descriptions, with safe fallback; possible model changes after session open remain a known
  precedence caveat (per-turn wire IDs still win, no live switch observed); this entry addresses
  the stale handoff finding. Both review tasks were cleaned and their new review grants revoked.
- **Gap 6 completed:** read protocols now type provider availability and usage reports, and
  the static asset mount receives a filesystem Path. Targeted Pyright: zero errors; 19 web/usage
  tests passed. Built and installed the wheel into a temporary directory; its `/`, `/static/app.js`
  and `/static/style.css` returned 200 with content, and all three assets are in the wheel.
  Claude review `ts_268ef8fa783d` passed. Both low findings were addressed: exported the protocols
  in `__all__` and documented the existing unpacked-filesystem installation assumption. The review
  task was force-cleaned and its new grant revoked. No new tests for annotation-only changes.
- **Gap 8 disposition completed:** keep the existing revoke-by-ID tool parameter and runtime
  validation. It is consistent with the current tool surface; a request-object conversion belongs
  to a future surface redesign. No interface or behavior was changed.
- **Gap 9 completed:** `start_task` documentation now explains that verification must leave no
  untracked files or ignore generated files such as `__pycache__/`. The dirty-overlap guard stays.
- **Gap 10 completed:** the AGENTS marker wording and 16384-byte rule are unchanged. The
  workspace-config context suite returned 19 passed and the same one unrelated failure:
  `test_claude_uses_sonnet_with_sonnet_subagents_and_six_worker_ceiling` expects `sonnet`, while
  the pre-existing local settings name `claude-fable-5-1[1m]`. Marker parity passes; neither the
  managed AGENTS files nor that local setting was edited.
- **Opportunity 1 completed:** `continue_task` optionally elicits retry/cancel from form-capable
  clients. It asks once, preserves the original state version, holds no transaction across the
  await, and never overrides recovery. Declined, dismissed, unsupported, failed or malformed
  elicitation retains the original recovery error. Ten changed-behavior cases cover these paths.
  The existing recovery suite and real stdio smoke passed (17 tools).
  Claude review `ts_ede197ae657b` returned CONCERN. All four findings were addressed: reproduced
  failed/malformed elicitation twice and restored the recovery error; tested URL-only clients;
  corrected the cycle-context comments (it is not a lock); updated the registration guide and
  this tracker. The review task was force-cleaned and its new grant revoked.
- **Opportunity 3 completed:** `repository_id` grouping spans providers within each repository
  and retains a null bucket for repository-free turns. Both SQL readers, MCP, CLI and dashboard
  support it without a migration; tables display the stable IDs. Individual usage rows also expose
  their repository ID. The 51 relevant tests passed, including three new cases for aggregation,
  read-only parity/filters, HTTP grouping and CLI output; JavaScript syntax passed.
  Claude review `ts_0998fd76b127` returned CONCERN. Dispositions: path labels are deferred as a UI
  enhancement beyond the requested ID grouping; documented the additive per-turn repository ID;
  added `repository_id` to the MCP tool description. The review task was force-cleaned and its
  new grant revoked.
- **Opportunity 5 completed:** new Grok turns use the first wire `_meta.modelId`, then the first
  `modelUsage` key, then the profile model. The wire-first case already agreed in current code;
  the fix moves the profile fallback after backend telemetry and keeps attribution/usage aligned.
  Raw backend IDs, token counts, unpriced usage and historical rows are preserved. Three new
  precedence cases and the runner's conflicting wire/backend-ID case passed (35 relevant tests).
  Claude review `ts_c94499c03785` passed. Both low findings were addressed: model-only replacement
  avoids repricing the profile fallback, and tests assert the unchanged source and unpriced cost.
  The review task was force-cleaned and its new grant revoked. No cumulative-usage inference was
  introduced; gap 2 remains blocked on a successful resumed Grok turn.
- Decision 5: the operator chose to defer second-class agents during planning.

### `~/personal_projects/taskspindle` — branch `provider-limits-usage-dashboard`

Original handoff baseline: four implementation commits on top of `main` (`8f4da48`), with
388 passed, 1 skipped and Ruff clean. The follow-on commits and current checks are listed above:

| Commit | What |
| --- | --- |
| `cb446aa` | Provider limit classification and availability, turn usage capture, `usage_report`, `taskspindle usage`, attribution fixes, 16 KiB diff page, orphan cleanup, schema 2 |
| `9efb94b` | Grok's `turn_completed` captured (vendor method `_x.ai/session_notification`), Claude session-record retry, repricing |
| `de4a38c` | Read-only web dashboard (`taskspindle web`), Starlette + uvicorn as explicit deps |
| `0ae8dcf` | Claude session modes (`plan` / `default`), Grok `--sandbox strict` for read-only turns, `MODE_SWITCH_ATTEMPT`, review prompts carry the diff, `taskspindle discover` |

Docs updated for all of it: `README.md`, `CHANGELOG.md` (Unreleased), `docs/tools.md`,
`docs/architecture.md`, `docs/configuration.md`, `docs/codex-registration.md`,
`docs/dashboard.md`.

Verified live against the real Claude Max and Grok seats, with this branch's code driving the
live store: consult on both providers; implement on each provider, cross-reviewed by the other,
accepted as a real commit; cancel; paged diff at the 16 KiB default; usage recorded on every turn
including model and cost estimate for Claude; the eight orphaned tasks from the first smoke test
cleaned and their grants revoked; `taskspindle discover` against the published registry; the
dashboard rendered against the live store. Worker logs confirm the Claude session modes and the
implement transcript shows the permission gate being consulted.

**The live store is already at schema 2** (the migration ran when this branch's server opened it).
The pinned 0.1.0 server still registered with Codex tolerates the extra tables; nothing needs
undoing.

### `~/git_repos/workspace-config` — `main`, commit `0e9460f`

"Make TaskSpindle the primary external delegation route": `global/codex/AGENTS.md` leads with
TaskSpindle and carries the `task_diff` paging rule; the review-gate block and the standalone
`~/.codex/AGENTS.gate.md` (unmanaged file, edited in place) agree; `config.baseline.toml` and its
test now say taskspindle enabled, rutherford disabled; `README.md` updated. The two merged
worktrees `codex-runtime-taskspindle` and `workspace-config-taskspindle` and their branches were
removed.

One test there still fails and is **not** from this work:
`test_claude_uses_sonnet_with_sonnet_subagents_and_six_worker_ceiling` reads
`global/claude/settings.json`, which has an uncommitted local change setting the model to
`claude-fable-5-1[1m]`. Decide whether that change is intended (then update the test) or revert it.

### `~/git_repos/codex-runtime` — unchanged

`global/taskspindle/taskspindle_sync.py` pins `PINNED_SHA = "8f4da48…"` (v0.1.0 `main`) and
clones from GitHub unless `TASKSPINDLE_SOURCE_ORIGIN` names another clone source. This is what
`~/.codex/config.toml` runs. Nothing on the new branch is live in Codex until this pin moves.
Final inspection also found an unrelated local change in `global/rutherford/config.toml` in
`codex-runtime`; this run did not edit or inspect its contents. Preserve it when handling decision 3.

## Decisions (operator only)

1. **Future publication and integration:** the previous branch's approved fast-forward merge,
   push and cleanup are complete. The release candidate remains local. The recorded Grok
   blocker has met its acceptance criterion; obtain approval for the exact final candidate's
   merge, push, v0.2.0 tag and GitHub release. No PR
   or external publication is implicitly approved by the implementation instruction.
2. **Activation:** after release approval and publication, merge the reviewed private changes,
   install the pinned 0.2.0 runtime and adapter, then change the active registration while
   preserving runtime-owned state. Follow private `docs/taskspindle-activation.md`, including
   hook registration/trust and the fresh Codex live cycle. Local source activation is not selected.
3. **Installed Rutherford removal:** source retirement is prepared. Actual installed runtime,
   links and active config removal require approval, a fresh job/process inventory and preservation
   of the existing private configuration edit. Keep shared services and the 0.1.0 rollback runtime.
5. **Second-class agents:** deferred. Initialization probes do not authorize profile edits,
   credentials, sign-ins or metered consults. Require an explicitly selected profile and reviewer.

## Decided (do not reopen)

- Prepare **0.2.0**, its immutable private runtime pin, registration and Rutherford source
  retirement in local branches. Publication and activation stay held if Grok continuation remains
  unresolved. The recorded stalls are now diagnosed and direct ACP plus isolated TaskSpindle
  continuation checks pass. Preparing metadata is not publishing a release; publication and
  activation still require explicit approval.
- Implement repository labels, Claude denial telemetry, dashboard search/review comparison,
  opt-in discovery probes and the private observed acceptance audit. Keep second-class calls,
  experimental Claude headroom, dependency bumps and attribution renaming deferred.
- **Never a fallback stays.** Quota and auth refusals are classified, recorded and reported
  (`capabilities`, `doctor`, `PROVIDER_UNAVAILABLE` on `start_task`); nothing is re-queued on
  another provider and nothing waits for a reset. The operator chose this over automatic
  switching. `ignore_provider_status` is the only override.
- **Dashboard is read-only.** No mutation endpoints, ever; the store is opened `mode=ro`.
- **Costs are estimates and say so.** Subscription seats are not billed per token; the figure is
  the published API rate for the same tokens. Grok's `costUsdTicks` is kept raw, never converted.
- **Grok read-only hardening is `--sandbox read-only` only** (superseding `strict` on 2026-09-05:
  `strict` allowed cwd writes and, on WSL, broke DNS inside the sandbox by denying the
  `/etc/resolv.conf` symlink target). `--permission-mode plan` and `--deny` were verified to do
  nothing on Grok 1.0.13's ACP endpoint. Do not add unverified flags.
- **Claude modes are `plan` for consult/review and `default` for implement.** `acceptEdits` was
  verified to run shell commands without asking; it is not an option.
- **`task_diff` default is 16 KiB** because Codex's `tool_output_token_limit` (8000 here)
  truncates tool output client-side while the receipt records the page as read. The maximum stays
  256 KiB.
- **Codex-native features are not used by TaskSpindle**, and mostly should not be: it is a server
  Codex calls. See `docs/codex-registration.md`.

## Gaps

Things that are known to be incomplete or unverified. Each has an acceptance criterion.

2. **Grok continuation acceptance is complete.**
   The three recorded post-load stalls all exhausted native Grok Responses transport retries
   after successful session load and prompt acceptance. A fresh direct initial turn and two
   fresh-process continuations pass. No local continuation fix or cumulative usage subtraction
   is warranted. The isolated 0.2.0 TaskSpindle initial turn and two continuations also pass,
   with retained context, one session, per-turn usage, aggregate usage and completed cleanup.
   Present the exact release and activation candidates for approval.
4. **Claude window headroom is observed, never polled.** `provider_windows` for Claude fills only
   from rate-limit events during a turn and from refusals. The SDK's `get_usage`
   (`usage_EXPERIMENTAL_MAY_CHANGE_DO_NOT_RELY_ON_THIS_API_YET` in the bundled
   `@anthropic-ai/claude-agent-sdk`) reads the seat's 5-hour and 7-day utilization but is not
   exposed over ACP. A small Node probe against the pinned SDK could fill the windows for `doctor`
   and the dashboard. Only worth doing if the experimental API is still there in the next adapter
   pin; do not build on it otherwise.
   **Follow-on result: prerequisite not met.** The installed SDK still declares the experimental
   method, but no next adapter pin has been selected. No probe was added and no adapter or
   runtime pin was changed. Recheck the API when that pin is chosen.
5. **Second-class profiles have never run live.** `docs/configuration.md` says so. The
   `claude-litellm` example in the managed `config.toml` is commented out. With `discover`'s
   Gemini or OpenCode block, or that example, run `taskspindle doctor` then one consult with
   `allow_metered: true`, and record in `docs/configuration.md` which one was tried and what
   `task_result.attribution` reported. Also check `providers.launch_command` for a `base = "grok"`
   profile: the sandbox flag is inserted before `agent`, which assumes the built-in argv shape.
   **Follow-on result:** explicitly deferred by the operator; no profile or credentials were changed.
7. **`starlette.testclient` deprecation.** `tests/test_web.py` imports `TestClient` inside a
   `warnings.catch_warnings()` block because Starlette 1.6 warns at import and the project runs
   pytest with `filterwarnings = ["error"]`. Watch this on the next Starlette bump.
   **Follow-on result:** Starlette remains at 1.6.0; the existing scoped import workaround is
   retained. The dashboard tests pass. No dependency bump is part of this work; revisit then.

## Opportunities

Opportunities 1 through 7 have implementation or measured disposition in State. The private
acceptance hook still needs approved activation and a native Codex acceptance check; it has not
been installed or trusted in the active runtime.

8. **Drop the `agent_info` misnomer.** `attribution.agent` is the adapter's name and version,
   as documented. No change is warranted unless the attribution shape is revisited; deferred.

## Verification commands

```sh
cd ~/personal_projects/taskspindle-release-readiness
uv run ruff check src tests && uv run pytest -q          # 418 passed, 1 skipped expected
uv build && unzip -l dist/*.whl | grep web/static         # dashboard assets ship in the wheel
uv run python .github/scripts/mcp_smoke.py                # 17 tools over a real stdio session

# Against the live store, with this branch's build (the pinned 0.1.0 runtime is untouched):
export TASKSPINDLE_CONFIG=/home/bsheffield/.config/taskspindle/config.toml
.venv/bin/taskspindle doctor                              # requires approved 0.2.0 adapter installation
.venv/bin/taskspindle usage --since 7d                    # tokens, outcomes, timings, windows
.venv/bin/taskspindle discover --all                      # registry agents installed here
.venv/bin/taskspindle web                                 # http://127.0.0.1:8765

cd ~/git_repos/workspace-config && python3 -m pytest -q tests/test_agent_context_configuration.py
```

A live end-to-end cycle is the definitive check: authorize a throwaway repository (give it a
`.gitignore` for `__pycache__`), consult on both providers, implement on one, `task_diff` at the
default page until covered, review on the other, `accept_task`, confirm the commit landed, then
`cleanup_task(force=true)` on everything and `revoke_repository`. The throwaway drivers used for
this session lived in the Claude Code scratchpad under `/tmp/claude-1000/…/scratchpad/wp0/`
(`drive.py`, `drive2.py`, `drive4.py`, `drive3.py`) and are not part of the repository; they were
FastMCP stdio clients calling the tools in that order. Rewrite one rather than looking for them.

## Findings worth remembering

- Grok 1.0.13 sends its turn summary on the JSON-RPC method `_x.ai/session_notification`, not
  `session/update`, and its own session log names the method differently. The ACP observer now
  accepts any `_<vendor>/…` method whose params are shaped like a session update.
- The Claude adapter 0.70.0 emits the turn's usage on the `session/prompt` response (ACP 0.12
  marks the field unstable) and forwards the SDK's `rate_limit_event` as `usage_update` with
  `_meta["_claude/rateLimit"]`. Failures arrive as JSON-RPC errors with `data.errorKind`.
- Claude Code writes the session record roughly 60 ms after the prompt response; the canonical model name
  still requires that file for the observed default configuration in adapter 0.70.0.
- The adapter takes its initial permission mode from the operator's `~/.claude/settings.json`
  regardless of `settingSources: []`; only `session/set_mode` changes it.
- Codex's `tool_output_token_limit` truncates MCP tool output silently; the model never sees
  that a page was cut.
